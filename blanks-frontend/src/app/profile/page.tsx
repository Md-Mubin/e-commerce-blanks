import { Layout } from '@/components';
import { ProfilePage } from '@/components/profilePage';
import React from 'react';

const page = () => {
	return (
		<Layout>
			<ProfilePage />
		</Layout>
	);
};

export default page;
